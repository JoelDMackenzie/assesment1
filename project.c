#include <stdio.h>

int main() {
    // Zeros first N elements of x
    void zero(int *x, int N) {
        int i=1; // Array index loop counter
        for(i = 0; i < N; i++)
        x[i] = 0; // Use array syntax
        return; // Optional
        }

    return 0;
}
